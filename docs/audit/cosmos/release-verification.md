# Celestial Atelier verification · 2026-09-13

The complete suite passes **298 tests, zero failures**. The full unedited output is in `tests.txt`. Eleven new checks cover the source sky/music contract and geometry, rhythm and cooling, the accelerated first growth cycle, local save/reclaim/rejection, all four local recipes and actual town effects, a legal two-player crossing-to-forge journey, shared scopes/replay/restart, two shipped DOM crafting journeys, and hosted D1 migration/alchemy across cold instances.

The source artwork remains 5,613,428 bytes with SHA-256 `86f7c110a82587eb50f302def2261b5ae9b431a41cdabf46b8f48a6c7a8f0668`. Extraction reproduces its letters, root dictionary, original fonts, numeral core, embedded ephemeris and three catalog star directions. The ephemeris body is retained unchanged inside a lexical module wrapper. This wrapper avoids conflicting module/default declarations introduced by the website bundler's CommonJS transform.

The Sites build completes all five build stages. `built-worker.json` records successful emitted-Worker requests for both game pages, the celestial modules, native font, full original artwork, health, player arrival and a Luma command against an isolated D1 database. Arrival also checks that the emitted authority returns a seven-body sky and an empty new workshop. No test identities or goods were written to the live database.

The local/shared DOM cases use HappyDOM with mocked canvas and audio. Rules tests use controlled time; the two-player journey obeys legal movement and funds the crossing before reaching mineral deposits. A separate ecology case explicitly moves pre-existing reserve stock to the pack to make room for growth. These checks do not establish visual quality, acoustic recognition, human usability or mobile performance. No new browser or human playtest was performed.

The shared state migration preserves existing players, materials and recovery. The D1 table migration is unchanged. Published access must retain the selected Site's existing audience and database binding. The game guide and broader audit keep the full MMO and other unimplemented obligations open.
